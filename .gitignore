__pycache__/
*.pyc
# Aegis runtime state lives in ~/.aegis, never in the repo — but ignore any
# stray local copies just in case.
.aegis/

# Spar arena (adversarial proof scratch) — promoted proofs land in tests/
.spar/
